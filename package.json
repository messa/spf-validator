{
  "name": "spf-validator",
  "version": "0.0.1",
  "description": "SPF validator website",
  "homepage": "https://github.com/messa/spf-validator",
  "repository": "github:messa/spf-validator",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "export-server": "npm run export && cd out && python3 -m http.server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "SPF"
  ],
  "author": "Petr Messner",
  "license": "MIT",
  "dependencies": {
    "next": "^6.0.3",
    "react": "^16.3.2",
    "react-dom": "^16.3.2"
  }
}
